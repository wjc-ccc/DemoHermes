# Logging — 日志工具

Agent 运行时的日志记录与调试工具。

## 文件说明

| 文件 | 职责 | 状态 |
|------|------|------|
| `TestLogtool.py` | 日志工具测试与验证脚本 | 🔲 待实现 |

## 计划功能

- 结构化日志输出（对话轮次、工具调用、记忆读写）
- 日志级别通过 `.env` 的 `LOG_LEVEL` 控制（默认 INFO）
- 与 `core/events.py` 配合，支持将事件同时写入日志文件
