### 1.channel是否必须单独解析函数 如何设计对应的message bus架构更合理？

这是不可或缺的一点，原始数据必须首先解析，根据不同channel提供的接口参考文档（sdk，api）然后获取对应的参数dict，转变为自己的message架构

### 问题2：types彼此之前到底如何定义  parser input 和 message到底如何转换

所以 需要有一个 IODTO 负责承接和简单解析对应的channel情况 这里需要重新定义 不需要太复杂 但是这个channel也必须提供一个函数 能够从INDTO 转化为Message（为什么多此一举？ 多这一步的好处是什么）。

Channel 产 InboundEvent（轻量运输），Gateway 产 Message（重对话事实）。

关键：Message 是在 Gateway 阶段由 InboundEvent 转出来的，不是 Channel 直接产 Message。

### 问题3：Session上下文存到哪里？

GateWay： 负责检索上下文 所以gateway的并发很关键（如何不卡顿）？ 如何测试并发能力？

- 内存dict：适合早期操作 好处就是快 小 方便切换
- JSONL：单机开发 早期存储
- Redis+SQL：Redis存储session_key--session_id + 分布式锁 SQL存正文

