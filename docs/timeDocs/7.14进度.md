# 7.14 进度

## 今日完成

1. **Agent Loop 设计方案** — [`docs/aiDocs/7.14 AgentLoop设计.md`](../aiDocs/7.14%20AgentLoop设计.md)
2. **可运行双队列 Gateway + Loop + CLI**
   - `agent/core/loop.py` — `AgentLoop.run_turn`
   - `agent/gateway/queues.py` — `in_dto` / `out_dto`
   - `agent/gateway/runner.py` — Gateway：入队、按 session_key 加锁串行、出站分发
   - `agent/gateway/session_store.py` — JSONL 默认落盘 + Memory 可选
   - `agent/channel/base.py` / `cli.py` / `wechat.py` — Channel 内字段映射
   - `python -m agent.gateway.cli --mock`

## 存储结论

- **现在**：JSONL（`data/sessions/index.json` + `transcripts/{id}.jsonl`）
- **下一步**：SQLite（单机生产 / FTS）
- **多机**：Redis（热索引+锁）+ SQL（正文）

## 运行

```bash
python -m agent.gateway.cli --mock
python -m agent.gateway.cli --mock --memory
python -m agent.gateway.cli          # 需有效 .env API Key
```

| 项 | 状态 |
|----|------|
| 边界 DTO + Session.add_message | ✅ |
| 双队列 + session_key 串行 | ✅ |
| JSONL SessionStore | ✅ |
| CLI / WeChat 解析骨架 | ✅ |
| Tool 内循环 | ❌ |
| Redis / SQLite 后端 | ❌ |
